rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to read and write to the auth collection
    // This stores the global password and master key
    match /auth/{document} {
      allow read, write: if true;
    }
  }
}
